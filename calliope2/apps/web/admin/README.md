# Admin (placeholder)

New React Admin SPA. Consolidates the existing Thoth (`/calliope/routes/thoth.py`) and Piccolo Admin (mounted at `/admin`) into one surface.

Scaffolding lands in Phase 8. Backend lives in `apps/api/calliope2/api/v3/admin/`.
